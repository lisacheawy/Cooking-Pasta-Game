package ch.makery.address.view
import ch.makery.address.model.ChecklistModel
import ch.makery.address.model.KitchenModel

class KitchenController {
  val model = new KitchenModel()
  val checklistModel = new ChecklistModel()
}